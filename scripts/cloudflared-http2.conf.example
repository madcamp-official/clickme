# Keep the tunnel on TCP/HTTP2 on networks where QUIC connections flap.
# This is a systemd drop-in for cloudflared.service.
[Service]
Environment=TUNNEL_TRANSPORT_PROTOCOL=http2
Restart=always
RestartSec=5s
